<?php namespace View; Use _; ?>                      <?php foreach ($posts as $year => $v) { ?>        <h2><?=$year?></h2>        <?php foreach ($v as $post) { ?>        <article>            <h1><a href="<?=url($post['link'])?>"><?=$post['title']?></a></h1>            <time datetime="<?=date('c', strtotime($post['date']))?>"><?=date('d.m.Y', strtotime($post['date']))?></time>            <p><?=$post['excerpt']?> <a href="<?=url($post['link'])?>">read more</a></p>        </article>        <?php } ?>         <?php } ?> 